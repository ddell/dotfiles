alias reload=". $HOME/.profile" # same as previous, after editing you have to source it for the new stuff to work add that one line to the aliases.sh

# standard
alias l="ls -AF"
alias la="ls -aliF"
alias ls="ls -h --group-directories-first --color=auto"
alias ll="ls -lFh"
alias myip="curl --silent http://tnx.nl/ip"
alias path='echo -e "${PATH//:/\n}"'

